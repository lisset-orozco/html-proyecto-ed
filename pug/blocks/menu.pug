ul
  each page in pages
    // if page == 'inicio'
    //  li: a(href='/') #{page}

    -page == currentPage ? active = 'active' : active = ''
    if page != 'inicio'
      li: a(href=page + '.html' class=active) #{page}